{
  "name": "paisa-wapas",
  "version": "0.1.0",
  "description": "Hinglish voice recovery agent for failed UPI autopay mandates, with a promise-to-pay ledger and RBI-style compliance guardrails",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "demo": "tsx src/demo/run.ts",
    "demo:live": "tsx src/demo/run.ts --live",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
